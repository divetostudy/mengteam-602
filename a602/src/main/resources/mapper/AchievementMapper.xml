<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.mapper.AchievementMapper">

    <insert id="add" parameterType="com.web.pojo.DO.Achievement" useGeneratedKeys="true" keyProperty="id">
        insert into achievement (title, content) VALUE ( #{title} , #{content})
    </insert>
    <update id="update" parameterType="com.web.pojo.DO.Achievement">
        update achievement
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content}</if>
        </set>
        where id = #{id}
    </update>
    <delete id="delete" parameterType="java.lang.Integer">
        delete from achievement where id = #{id}
    </delete>
</mapper>
