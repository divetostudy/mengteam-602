<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.mapper.ProjectMapper">

    <insert id="add" keyProperty="id" useGeneratedKeys="true">
        insert into project (title, content, url) VALUE
        (#{title}, #{content}, #{url})
    </insert>
    <update id="update">
        update project
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            url = #{url}
        </set>
        where id = #{id}
    </update>
    <delete id="delete">
        delete from project where id = #{id}
    </delete>
    <select id="getUrl" resultType="java.lang.String">
        select url from project where id = #{id}
    </select>
    <select id="getById" resultType="com.web.pojo.DO.Project">
        select * from project where id = #{id}
    </select>
</mapper>
